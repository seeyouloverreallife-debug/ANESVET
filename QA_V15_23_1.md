# QA — ANESVET V15.23.1

## Static checks
- JavaScript syntax: PASS for all JS files
- HTML duplicate IDs: PASS — 0 duplicate IDs
- App / index / manifest / service-worker version: V15.23.1 consistent
- `clinical-validation.js`: byte-identical to V15.23.0
- `medication-reconciliation.js`: byte-identical to V15.23.0
- `finalization.js`: byte-identical to V15.23.0
- `clinical-workflow.js`: byte-identical to V15.23.0

## Chromium interaction regression
Mobile viewport: 390 × 844 px; virtual-keyboard resize simulated by changing height to 520 px while `#patientName` remained focused.

Observed:
- Initial Patient Master cards: 0
- Initial status: `Search when needed`
- After saving one patient and returning to Patient: 0 cards while query is blank; count badge `1 ACTIVE`
- New patient: search query blank; 0 cards
- Search `ScrollTest`: 1 card; `1 matches`
- Select patient: query cleared; 0 cards; Linked Patient banner remains visible
- Focus/resize scroll position: `scrollY 703 → 703` (delta 0)
- Clinical validation matrix: PASS 15/15
- Page errors: 0
- Console errors: 0

## Regression rationale
V15.23.0 aligned the active workflow tab with `Element.scrollIntoView()` on every window resize. Mobile virtual keyboards resize the visual/layout viewport, so that handler could pull the document back toward the workflow bar at the top. V15.23.1 aligns the active tab by modifying only `.workflow-tabs.scrollLeft`, leaving vertical document scroll untouched.

## Real-device acceptance still recommended
- Android Chrome/PWA: type through Name, HN, Breed and Current BW with the keyboard open.
- iPad Safari/PWA: repeat the same flow and rotate once.
- Confirm the focused field stays visible and the page does not jump to the workflow bar.
- Verify PWA update from V15.23.0 → V15.23.1 once on the deployed origin.
