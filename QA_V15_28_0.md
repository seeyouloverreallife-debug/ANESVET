# QA — ANESVET V15.28.0

## Static checks
- JavaScript syntax: PASS
- Duplicate HTML IDs: 0
- Service-worker asset paths: PASS
- Runtime version markers: V15.28.0

## Autosave benchmark — Chromium DOM runtime harness
The harness runs the production HTML/JS in Chromium with an in-memory Storage shim because this environment blocks local-origin navigation.

### Rapid generic text input
10 consecutive `patientName` input events:

- **V15.27:** 10 immediate current-state writes + 1 debounced write
- **V15.28:** 0 immediate current-state writes + 1 debounced write after 450 ms

V15.28 UI state immediately after typing: `Unsaved changes`; after debounce: `Saved locally`. Latest value was present in persisted current state.

### Background / pagehide flush
A new value was entered and `pagehide` was dispatched before the 450 ms debounce completed.

- persisted latest value: PASS
- immediate current-state write on flush: PASS

### Critical OR action
An active intraoperative case was created in the runtime harness and a vital set was recorded.

- record count incremented immediately: PASS
- current-state write occurred immediately: PASS
- active safety checkpoint write occurred immediately: PASS
- persisted record count matched in-memory record count: PASS

### Reset stale-autosave race regression
Reproduction: enter `OldShouldNotReturn`, immediately Reset/New Case, then wait 700 ms (longer than the 450 ms autosave delay).

- **V15.27:** blank immediately after Reset, then old value returned after the pending autosave fired — reproduced
- **V15.28:** blank immediately after Reset and still blank after 700 ms — PASS
- V15.28 reset path produced only the fresh current-state write in the harness — PASS

### Discrete vs continuous persistence
- Species select change: persisted immediately — PASS
- 10 rapid Procedure text inputs: 0 immediate current-state writes, 1 debounced write, latest value persisted — PASS

### Clinical validation
- Clinical validation matrix: **PASS 15/15**
- Runtime JS errors captured by harness: **0**

## Regression scope
The following modules are intentionally unchanged from V15.27.0 and are verified byte-identical in packaging QA:
- `clinical-validation.js`
- `clinical-workflow.js`
- `drug-dose-reference.js`
- `protocol-review.js`
- `medication-reconciliation.js`
- `finalization.js`

## Limitation
Full installed-PWA/service-worker lifecycle still requires device validation on Android Chrome and iPad Safari because this execution environment blocks direct local-origin browser navigation.
