ANESVET V14.9.0 — browser/DOM E2E status
Date: 2026-09-22

NOT CLAIMED in this runtime.

The jsdom/fake-indexeddb dependencies required by the DOM harness are unavailable. `npm ci --ignore-scripts --no-audit --no-fund` was attempted but timed out in the container environment.

Static syntax/regression/mobile contracts and non-DOM clinical helper/workflow model tests passed. Acceptance testing is still required on the actual deployment browsers/devices, especially:
- iPadOS Safari / installed PWA, portrait + landscape
- iPhone Safari / installed PWA if used
- Android Chrome / installed PWA if used
- screen wake-lock behavior during an active case
- offline launch after the service worker has been installed once
- keyboard/modal behavior during OR LIVE data entry
